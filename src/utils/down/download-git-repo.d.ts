/**
 * Expose `download`.
 */
/**
 * Download `repo` to `dest` and callback `fn(err)`.
 *
 * @param {String} repo
 * @param {String} dest
 * @param {Object} opts
 * @param {Function} fn
 */
export declare function download(repo: any, dest: any, opts: any, fn: any): Promise<void>;
